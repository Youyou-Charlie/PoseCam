# BLOCKED.md · 待裁决清单（随交付提交）

## B1 · 规格测试代码笔误：`a.x` 应为 `a[0].x`（Task 1）
- 位置：`docs/superpowers/plans/2026-07-17-pwa-v2-real-functions.md` Task 1 Step 1 测试代码。
- 问题：`update()` 按接口返回 landmarks **数组**，规格测试却写 `a.x`/`b.x`/`c.x`（数组上取 `.x` 恒为 `undefined`），逐字照抄必然 AssertionError，无法满足「测试全绿」硬指标。
- 处理：测试文件 `pwa/tests/poseSmooth.test.js` 勘误为 `a[0].x`/`b[0].x`/`c[0].x`，断言值与强度与规格逐字一致（strictEqual 0.5 / |0.55|<1e-6 / <0.6），并新增 null 淡出、reset、拷贝语义、默认参数的真实断言。规格文件本身未改动（只读）。
- 需裁决：是否认可此勘误方式（替代方案：让 update 返回带首点快捷属性的非标准数组，不采用）。

## B2 · 规格与现状文案不一致：Task 6 目标文案
- 规格写现状为「✓ 已自动保存到相册」，实际 `pwa/index.html` 现状为「✓ 已自动保存」（无「到相册」二字），故验收命令 `grep -rn "已自动保存到相册" pwa/` 天然无命中（空过）。
- 处理：按规格**意图**执行——将该误导性文案改为「✓ 已暂存 · 点保存写入相册」，不以空过为满足。规格文件未改动。
